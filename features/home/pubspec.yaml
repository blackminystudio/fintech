name: home
description: Home feature (dashboard, main feed, etc.)
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.2.3 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  core:
    path: ../../core
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  get_it: ^8.0.3
  # injectable: ^2.5.0

dev_dependencies:
  build_runner: ^2.4.7
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.7.0
