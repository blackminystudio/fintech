# melos_managed_dependency_overrides: auth,config,core,home
dependency_overrides:
  auth:
    path: ../features/auth
  config:
    path: ../config
  core:
    path: ../core
  home:
    path: ../features/home
